# FI Logo Finder

Web app to find likely PNG logos (<11KB) for U.S. financial institutions.

## Run

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Open `http://localhost:8000`.
